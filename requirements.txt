# Core dependencies
letta>=0.5.0
anthropic>=0.25.0

# Voice synthesis
edge-tts>=6.1.0

# Audio playback (platform-specific, optional)
# For Linux: apt-get install mpg123 or mpv
# For macOS: afplay is built-in
# For Windows: Windows Media Player is built-in
